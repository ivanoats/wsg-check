# Ignore GitHub-managed instruction/agent files – these contain embedded code
# samples with intentionally non-standard formatting and must not be touched
# by Prettier.
.github/

# Vendored W3C WSG spec data — kept byte-identical to upstream release tags.
src/config/spec/*.json
